---
title: "Verification: How We Know the Python Matches the CBO"
format: live-html
reference-location: margin
order: 9
---

## The claim to check

The Congressional Budget Office wrote its model in a specialized statistics language called EViews.^[EViews is a commercial program built for economists working with time series. It is powerful, but most people don't have it, and you cannot easily read or run it for free.] Everything on this site is a *rewrite* of that model into Python, line by line.

Translation introduces mistakes: a misread number, a step in the wrong order. This page shows the translation is faithful.

## Replication

The check is the one scientists use everywhere.

::: {#nte-replication .callout-note title="Replication (verification)"}
**Replication** means reproducing a result that is *already known to be correct* using your own method. If your method produces the known answer, you have strong evidence your method is sound. If it doesn't, you have a bug to find.
:::

Replication needs a set of trusted numbers we did not produce ourselves.

::: {#nte-benchmark .callout-note title="Benchmark"}
A **benchmark** is the trusted reference you measure yourself against. Here, the benchmark is the **CBO's own published table of historical growth rates**: numbers the CBO printed in the documentation that ships with their official program. They are the answer key.
:::

The benchmark is the CBO's answer key. The replication is running our Python and checking our answers, cell by cell, against it.

## What the answer key looks like

The CBO's table reports how fast the economy's *potential* grew during different stretches of American history. Each **row** is a different quantity: potential output itself, the potential labor force, potential hours worked, capital services, total factor productivity, and so on. Each **column** is a stretch of history: the postwar boom of 1950-1973, the slowdown of the 1970s, the 1990s tech boom, out to a final column for the whole 1950-2016 span.

That's 14 rows across 7 columns: exactly **98 numbers**. Our job is to land on all 98.

Three of those rows. The values are growth rates in percent per year. Read "3.98" as "grew about 3.98% every year, on average, during that period." Compounded over a couple of decades, that roughly *doubles* the size of that piece of the economy.

| Row (percent per year) | 1950-1973 | 1974-1981 | 1982-1990 | 1991-2001 | 2008-2016 |
|---|---|---|---|---|---|
| Potential Output | 3.98 | 3.16 | 3.42 | 3.26 | 1.45 |
| Potential Labor Force | 1.58 | 2.51 | 1.68 | 1.23 | 0.54 |
| Potential TFP | 1.91 | 0.89 | 1.28 | 1.46 | 0.74 |

Every one of those numbers is *both* what our Python computed *and* what the CBO published. They are the same to two decimals. [The economic story is in these rows too: potential output growth fades from a brisk ~4% in the golden age to under 1.5% in the slow recovery after 2008.]{.column-margin}

## How the check works in code

One file does nothing but build that table and hold it up against the CBO's answer key. The answer key is stored literally in the code: a Python dictionary called `TARGET`, where each entry is one row of the CBO's published numbers:

```python
# CBO README values, used only for diagnostic comparison.
TARGET = {
    "Potential Output":        [3.98, 3.16, 3.42, 3.26, 2.40, 1.45, 3.21],
    "Potential Labor Force":   [1.58, 2.51, 1.68, 1.23, 0.97, 0.54, 1.45],
    "Potential LF Productivity":[2.37, 0.64, 1.71, 2.01, 1.42, 0.91, 1.73],
    ...
}
```

A short function lines up *our* number next to *their* number for every cell and subtracts:

```python
def compare_to_target(table):
    columns = list(table.columns)
    rows = []
    for name, target in TARGET.items():
        if name not in table.index:
            continue
        model = table.loc[name].values
        diff = model - np.array(target)
        ...
```

For each row name, grab the model's values, grab the CBO's values, and compute `diff = model − target`. That `diff` is the gap between us and the answer key for every cell. If the model is faithful, every `diff` is essentially zero.

## The verdict

Across all 98 cells:

- The **worst** single cell is off by **0.013 percentage points**.
- The **average** miss is **0.003 percentage points**.

For scale: a growth rate of 3.21% versus 3.20% is a difference of 0.01, about the size of our *worst* error. On a $20-trillion economy, the typical gap between our number and the CBO's is far thinner than the rounding you would do by hand. For practical purposes, the two models are identical.

@fig-ver shows it in one picture. Each dot is one cell of the table. The horizontal position is the CBO's published value; the vertical position is our Python value. The diagonal line is perfect agreement: a dot on it means "our number equals theirs exactly." Every dot sits on the line.

![Every cell of the CBO's published growth table, our Python value vs. the CBO's, all on the 45-degree line.](images/verification.png){#fig-ver width=70%}

## Why it isn't *perfectly* zero

We did not hit the answer key dead-on in every cell, and we should not expect to.

Two harmless things create the hairline gaps. First, **rounding**: the CBO published its table rounded to two decimals, and rounding loses a sliver of precision. Second, EViews and Python don't do the exact same arithmetic deep inside certain steps, especially the **smoothing filter**, a tool the model uses to draw a smooth trend line through bumpy data. Two programs can draw very slightly different smooth lines from the same data, and that tiny difference ripples forward into the final growth rates.

That is the entire source of the discrepancy. There is no hidden disagreement about *how the economy works*, only tiny numerical differences from two programs grinding through the same long calculation. "Matches" here means *agrees to three-thousandths of a percentage point on average*, not *bit-for-bit identical*. That is the right standard for a faithful translation.

## Where this leaves you

The question these pages started with: how much could the U.S. economy produce if it were running at full health? The answer is built from a production function fed by labor, by capital weighted to its rental cost, and by the TFP leftover, aggregated into a single number for potential GDP, with a forecast you can steer.

It is not a black box you have to take on faith. It reproduces the CBO's published answer key to within a rounding error, cell by cell, across seventy years of history.

Go back to the [forecast page](forecast.qmd) and break it. Push productivity down, assume an older, slower-growing workforce, and watch the economy's ceiling bend. Every number it gives you comes from the same model that just reproduced the CBO's answer key.
